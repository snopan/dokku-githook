#!/usr/bin/env bash

declare HOOK_DATA_PATH="$PLUGIN_AVAILABLE_PATH/githook/data/hooks"

addHook() {
	declare hook=$1
	echo "$hook" > "$HOOK_DATA_PATH"
	canAddHook
}

canAddHook() {
	while read hook; do
		echo "$hook"
	done < "$HOOK_DATA_PATH"
}

removeHook() {
	echo "removed"
}