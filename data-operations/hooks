#!/usr/bin/env bash

declare HOOK_DATA_PATH="$PLUGIN_AVAILABLE_PATH/githook/data/hooks"

addHook() {
	echo "$1" >> "$HOOK_DATA_PATH"
}

canAddHook() {
	while read hook; do
		[[ "$1" == "$hook" ]] && return 1
	done < "$HOOK_DATA_PATH"
	return 0
}

removeHook() {
	echo "removed"
}