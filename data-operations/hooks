#!/usr/bin/env bash

declare HOOK_DATA_PATH="$PLUGIN_PATH/data/hooks"

addHook() {
	declare hook=$1
	echo "$hook" > "$HOOK_DATA_PATH"
	canAddHook
}

canAddHook() {
	while read hook; do
		echo "$p"
	done < "$HOOK_DATA_PATH"
}

removeHook() {
	echo "removed"
}