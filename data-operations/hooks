#!/usr/bin/env bash

declare HOOK_DATA_PATH="$PLUGIN_AVAILABLE_PATH/githook/data/hook"

addHook() {
	declare hook = $1
	echo "$hook" > HOOK_DATA_PATH
}

canAddHook() {
	while read hook; do
		echo "$p"
	done < HOOK_DATA_PATH
}

removeHook() {

}