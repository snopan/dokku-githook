#!/usr/bin/env bash

installGo() {
	wget https://dl.google.com/go/go1.17.2.linux-amd64.tar.gz
	tar -xzf go1.17.2.linux-amd64.tar.gz
}

# Install golang binaries if it doesn't exist
# installGo

# Create all storage files
mkdir "$PLUGIN_CORE_AVAILABLE_PATH/data"
echo > "$PLUGIN_CORE_AVAILABLE_PATH/data/hooks"
echo > "$PLUGIN_CORE_AVAILABLE_PATH/data/repos"
echo > "$PLUGIN_CORE_AVAILABLE_PATH/data/links"